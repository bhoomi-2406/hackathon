<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Technokratos - College Event Management System</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&display=swap" rel="stylesheet">
    <style>
        /* Base Styles */
        :root {
            --primary-color: #6366f1; /* Tailwind 'indigo-500' */
            --secondary-color: #764ba2; /* A complementary purple */
            --text-color-dark: #1e293b; /* Tailwind 'slate-800' */
            --text-color-light: #64748b; /* Tailwind 'slate-500' */
            --bg-light: #f8fafc; /* Tailwind 'slate-50' */
            --bg-white: #ffffff;
            --shadow-light: 0 2px 20px rgba(0, 0, 0, 0.1);
            --shadow-medium: 0 10px 30px rgba(0, 0, 0, 0.1);
            --shadow-heavy: 0 20px 40px rgba(0, 0, 0, 0.1);
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Poppins', sans-serif;
            line-height: 1.6;
            color: var(--text-color-light);
            overflow-x: hidden;
            background-color: var(--bg-white);
        }

        /* Reusable Components */
        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 1.5rem; /* Slightly reduced padding for smaller screens */
        }

        .section-title {
            text-align: center;
            font-size: 2.8rem; /* Slightly larger */
            margin-bottom: 3.5rem; /* More spacing */
            color: var(--text-color-dark);
            font-weight: 700; /* Bolder title */
        }

        .cta-button {
            display: inline-block;
            padding: 15px 35px; /* Slightly more padding */
            background: var(--bg-white);
            color: var(--primary-color);
            text-decoration: none;
            border-radius: 50px;
            font-weight: 600; /* Medium bold */
            transition: all 0.3s ease;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
            text-transform: uppercase; /* Make it uppercase */
            letter-spacing: 0.5px;
        }

        .cta-button:hover {
            transform: translateY(-5px); /* More pronounced lift */
            box-shadow: 0 18px 45px rgba(0, 0, 0, 0.3); /* Larger shadow */
        }

        /* --- Navigation --- */
        .navbar {
            position: fixed;
            top: 0;
            width: 100%;
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
            padding: 0.9rem 0; /* Slightly less padding */
            z-index: 1000;
            transition: all 0.3s ease;
            box-shadow: var(--shadow-light);
        }

        .nav-container {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 0 1.5rem; /* Use container padding */
        }

        .logo {
            font-size: 2rem; /* Larger logo */
            font-weight: 700;
            color: var(--primary-color);
            text-decoration: none;
            text-transform: uppercase; /* Uppercase for brand */
        }

        .nav-menu {
            display: flex;
            list-style: none;
            gap: 2.5rem; /* More spacing */
        }

        .nav-link {
            text-decoration: none;
            color: var(--text-color-dark);
            font-weight: 500;
            transition: color 0.3s ease, transform 0.2s ease;
            position: relative;
            padding: 5px 0; /* Add padding for larger click area */
        }

        .nav-link:hover {
            color: var(--primary-color);
            transform: translateY(-2px); /* Slight lift on hover */
        }

        .nav-link::after {
            content: '';
            position: absolute;
            width: 0;
            height: 3px; /* Thicker underline */
            bottom: -8px; /* Lower underline */
            left: 0;
            background-color: var(--primary-color);
            transition: width 0.3s ease;
        }

        .nav-link:hover::after {
            width: 100%;
        }

        .mobile-menu {
            display: none;
            flex-direction: column;
            cursor: pointer;
            z-index: 1001; /* Ensure it's above other content when open */
        }

        .mobile-menu span {
            width: 30px; /* Slightly larger burger icon */
            height: 4px; /* Thicker lines */
            background: var(--text-color-dark);
            margin: 4px 0; /* More spacing */
            transition: 0.4s; /* Smoother transition */
        }

        /* Hamburger animation */
        .mobile-menu.active span:nth-child(1) {
            transform: rotate(-45deg) translate(-6px, 6px);
        }
        .mobile-menu.active span:nth-child(2) {
            opacity: 0;
        }
        .mobile-menu.active span:nth-child(3) {
            transform: rotate(45deg) translate(-6px, -6px);
        }

        /* --- Hero Section --- */
        .hero {
            height: 100vh;
            background: linear-gradient(135deg, var(--primary-color) 0%, var(--secondary-color) 100%);
            display: flex;
            align-items: center;
            justify-content: center;
            text-align: center;
            color: white;
            position: relative;
            overflow: hidden;
        }

        .hero::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            /* Using a more subtle, repeatable SVG pattern for background texture */
            background: url("data:image/svg+xml,%3Csvg width='60' height='60' viewBox='0 0 60 60' xmlns='http://www.w3.org/2000/svg'%3E%3Cg fill='none' fill-rule='evenodd'%3E%3Cg fill='%23ffffff1a' fill-opacity='0.08'%3E%3Cpath d='M36 34v-4h-2v4h-4v2h4v4h2v-4h4v-2h-4zm0-30V0h-2v4h-4v2h4v4h2V6h4V4h-4zm0 60v-4h-2v4h-4v2h4v4h2v-4h4v-2h-4zM0 4L4 0h2L0 6V4zm0 60L4 56h2L0 66v-2zm60 0l-4-4h-2l4 6v-2zm0-60L56 0h-2l4 6V4zm-48 0L12 0h-2l4 6V4zm-48 60L12 56h-2l4 6v-2zm48 0L48 56h-2l4 6v-2zm48 0L48 56h-2l4 6v-2zM0 4L4 0h2L0 6V4zm0 60L4 56h2L0 66v-2zM0 60L4 56h2L0 66v-2zM0 4L4 0h2L0 6V4zM60 4L56 0h-2l4 6V4z'/%3E%3C/g%3E%3C/g%3E%3C/svg%3E");
            background-size: 60px 60px; /* Adjust size as needed */
        }


        .hero-content {
            max-width: 900px; /* Wider content area */
            padding: 0 1.5rem;
            position: relative;
            z-index: 2;
        }

        .hero h1 {
            font-size: 4.2rem; /* Even larger for impact */
            margin-bottom: 1.5rem;
            animation: fadeInUp 1s ease;
            font-weight: 700;
            text-shadow: 2px 2px 4px rgba(0,0,0,0.2);
        }

        .hero p {
            font-size: 1.4rem; /* Larger text */
            margin-bottom: 2.5rem;
            opacity: 0.95; /* More visible */
            animation: fadeInUp 1s ease 0.2s both;
        }

        /* --- Features Section --- */
        .features {
            padding: 100px 0;
            background: var(--bg-light);
        }

        .features-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(320px, 1fr)); /* Min width 320px */
            gap: 2.5rem; /* More spacing */
            margin-top: 3.5rem;
        }

        .feature-card {
            background: var(--bg-white);
            padding: 2.5rem; /* More padding */
            border-radius: 20px; /* More rounded corners */
            text-align: center;
            box-shadow: var(--shadow-medium);
            transition: transform 0.4s ease, box-shadow 0.4s ease;
            border-top: 5px solid var(--primary-color); /* Add a subtle colored border */
        }

        .feature-card:hover {
            transform: translateY(-12px); /* More lift */
            box-shadow: 0 15px 45px rgba(0, 0, 0, 0.15); /* Slightly larger shadow */
        }

        .feature-icon {
            font-size: 3.5rem; /* Larger icons */
            color: var(--primary-color);
            margin-bottom: 1.5rem;
        }

        .feature-card h3 {
            font-size: 1.7rem; /* Larger heading */
            margin-bottom: 1rem;
            color: var(--text-color-dark);
            font-weight: 600;
        }

        .feature-card p {
            font-size: 1.05rem; /* Better readability */
            color: var(--text-color-light);
        }

        /* --- About Section --- */
        .about {
            padding: 100px 0;
            background: var(--bg-white);
        }

        .about-content {
            display: grid;
            grid-template-columns: 1fr 1.2fr; /* Image slightly larger */
            gap: 5rem; /* More space */
            align-items: center;
        }

        .about-text h2 {
            font-size: 2.8rem;
            margin-bottom: 2rem;
            color: var(--text-color-dark);
            font-weight: 700;
        }

        .about-text p {
            font-size: 1.15rem; /* Slightly larger text */
            margin-bottom: 1.8rem;
            color: var(--text-color-light);
        }

        .about-image {
            text-align: center;
        }

        .about-image img {
            max-width: 100%;
            height: auto; /* Ensure aspect ratio is maintained */
            border-radius: 20px; /* More rounded */
            box-shadow: var(--shadow-heavy);
            object-fit: cover; /* Ensures image fills container without distortion */
        }

        /* --- Events Section --- */
        .events {
            padding: 100px 0;
            background: var(--bg-light);
        }

        .events-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(360px, 1fr)); /* Min width 360px */
            gap: 2.5rem;
            margin-top: 3.5rem;
        }

        .event-card {
            background: var(--bg-white);
            border-radius: 20px;
            overflow: hidden;
            box-shadow: var(--shadow-medium);
            transition: transform 0.4s ease, box-shadow 0.4s ease;
            display: flex; /* Use flexbox for image and content */
            flex-direction: column;
            border-bottom: 5px solid var(--primary-color);
        }

        .event-card:hover {
            transform: translateY(-8px);
            box-shadow: 0 15px 45px rgba(0, 0, 0, 0.15);
        }

        .event-image {
            height: 220px; /* Taller images */
            background: linear-gradient(45deg, var(--primary-color), var(--secondary-color));
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-size: 3.5rem; /* Larger icon */
        }

        .event-content {
            padding: 1.8rem; /* More padding */
            flex-grow: 1; /* Allow content to grow */
        }

        .event-date {
            color: var(--primary-color);
            font-weight: 700; /* Bolder date */
            font-size: 0.95rem; /* Slightly larger */
            margin-bottom: 0.5rem;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .event-title {
            font-size: 1.5rem; /* Larger title */
            margin: 0.7rem 0;
            color: var(--text-color-dark);
            font-weight: 600;
        }

        .event-content p {
            font-size: 1.05rem;
            color: var(--text-color-light);
        }


        /* --- Queries Section --- */
        .queries {
            padding: 100px 0;
            background: var(--bg-white);
        }

        .contact-form {
            max-width: 650px; /* Slightly wider form */
            margin: 0 auto;
            background: var(--bg-light);
            padding: 3rem; /* More padding */
            border-radius: 20px; /* More rounded */
            box-shadow: var(--shadow-medium);
        }

        .form-group {
            margin-bottom: 1.8rem; /* More spacing */
        }

        .form-group label {
            display: block;
            margin-bottom: 0.6rem; /* More spacing */
            color: var(--text-color-dark);
            font-weight: 600; /* Bolder label */
            font-size: 1.05rem;
        }

        .form-group input,
        .form-group textarea {
            width: 100%;
            padding: 14px; /* More padding */
            border: 2px solid #e2e8f0;
            border-radius: 10px; /* More rounded */
            font-size: 1rem;
            transition: border-color 0.3s ease, box-shadow 0.3s ease;
            color: var(--text-color-dark);
            background-color: var(--bg-white);
        }

        .form-group input:focus,
        .form-group textarea:focus {
            outline: none;
            border-color: var(--primary-color);
            box-shadow: 0 0 0 3px rgba(99, 102, 241, 0.2); /* Focus ring effect */
        }

        .submit-btn {
            background: var(--primary-color);
            color: white;
            padding: 14px 35px; /* More padding */
            border: none;
            border-radius: 10px; /* More rounded */
            font-size: 1.1rem; /* Larger text */
            cursor: pointer;
            transition: background 0.3s ease, transform 0.2s ease;
            width: 100%; /* Full width button */
            font-weight: 600;
        }

        .submit-btn:hover {
            background: #5856eb; /* Slightly darker hover */
            transform: translateY(-2px); /* Slight lift */
        }

        /* --- Footer --- */
        .footer {
            background: var(--text-color-dark);
            color: white;
            text-align: center;
            padding: 2.5rem 0; /* More padding */
            font-size: 0.95rem;
        }

        .footer p {
            margin: 0; /* Remove default paragraph margin */
        }

        /* --- Animations --- */
        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(40px); /* More pronounced */
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* --- Responsive Design --- */
        @media (max-width: 992px) {
            .hero h1 {
                font-size: 3.5rem;
            }
            .hero p {
                font-size: 1.2rem;
            }
            .about-content {
                grid-template-columns: 1fr;
                gap: 3rem;
                text-align: center;
            }
            .about-image {
                order: -1; /* Image appears above text on smaller screens */
            }
            .section-title {
                font-size: 2.2rem;
            }
        }

        @media (max-width: 768px) {
            .nav-menu {
                position: fixed;
                left: -100%;
                top: 70px; /* Adjust based on navbar height */
                flex-direction: column;
                background-color: var(--bg-white);
                width: 100%;
                text-align: center;
                transition: 0.4s ease-in-out; /* Smoother transition */
                box-shadow: 0 10px 27px rgba(0, 0, 0, 0.05);
                padding: 2rem 0;
                max-height: calc(100vh - 70px); /* Limit height to prevent overflow */
                overflow-y: auto; /* Allow scrolling if menu is long */
            }

            .nav-menu.active {
                left: 0;
            }

            .nav-link {
                margin: 1rem 0; /* More spacing in mobile menu */
                font-size: 1.2rem;
            }

            .mobile-menu {
                display: flex;
            }

            .hero h1 {
                font-size: 2.8rem;
            }

            .hero p {
                font-size: 1.1rem;
            }

            .features-grid,
            .events-grid {
                grid-template-columns: 1fr;
            }
            .feature-card, .event-card, .contact-form {
                padding: 1.8rem; /* Slightly less padding on mobile */
            }
        }

        @media (max-width: 480px) {
            .hero h1 {
                font-size: 2.2rem;
            }
            .section-title {
                font-size: 1.8rem;
            }
            .cta-button {
                padding: 12px 25px;
                font-size: 0.9rem;
            }
        }
    </style>
</head>
<body>
    <nav class="navbar" role="navigation" aria-label="Main Navigation">
        <div class="nav-container container">
            <a href="#home" class="logo" aria-label="Technokratos Home">Technokratos</a>
            <ul class="nav-menu" role="menubar">
                <li role="none"><a href="#home" class="nav-link" role="menuitem">Home</a></li>
                <li role="none"><a href="#about" class="nav-link" role="menuitem">About Us</a></li>
                <li role="none"><a href="#events" class="nav-link" role="menuitem">Events</a></li>
                <li role="none"><a href="#queries" class="nav-link" role="menuitem">Queries</a></li>
                <li role="none"><a href="login.php" class="nav-link" role="menuitem">Login</a></li> </ul>
            <button class="mobile-menu" aria-label="Toggle mobile menu" aria-expanded="false" aria-controls="nav-menu">
                <span></span>
                <span></span>
                <span></span>
            </button>
        </div>
    </nav>

    <section id="home" class="hero" aria-labelledby="hero-title">
        <div class="hero-content">
            <h1 id="hero-title">Welcome to Technokratos</h1>
            <p>Your Ultimate College Event Management System - Streamline, Organize, and Execute Events with Excellence</p>
            <a href="#events" class="cta-button">Explore Events</a>
        </div>
    </section>

    <section class="features" aria-labelledby="features-title">
        <div class="container">
            <h2 id="features-title" class="section-title">Why Choose Technokratos?</h2>
            <div class="features-grid">
                <div class="feature-card">
                    <div class="feature-icon" aria-hidden="true">
                        <i class="fas fa-calendar-alt"></i>
                    </div>
                    <h3>Event Planning</h3>
                    <p>Comprehensive event planning tools to organize every detail of your college events seamlessly.</p>
                </div>
                <div class="feature-card">
                    <div class="feature-icon" aria-hidden="true">
                        <i class="fas fa-users"></i>
                    </div>
                    <h3>Team Management</h3>
                    <p>Efficiently manage event teams, assign roles, and coordinate activities across departments.</p>
                </div>
                <div class="feature-card">
                    <div class="feature-icon" aria-hidden="true">
                        <i class="fas fa-chart-line"></i>
                    </div>
                    <h3>Analytics & Reports</h3>
                    <p>Get detailed insights and analytics on event performance, attendance, and feedback.</p>
                </div>
            </div>
        </div>
    </section>

    <section id="about" class="about" aria-labelledby="about-title">
        <div class="container">
            <div class="about-content">
                <div class="about-text">
                    <h2 id="about-title">About Technokratos</h2>
                    <p>Technokratos is a cutting-edge event management system designed specifically for colleges and universities. We understand the unique challenges of organizing academic events, cultural festivals, technical symposiums, and student activities.</p>
                    <p>Our platform combines modern technology with intuitive design to provide a comprehensive solution for event organizers, administrators, and participants.</p>
                    <p>From small workshops to large-scale festivals, Technokratos empowers your institution to create memorable experiences that engage students, faculty, and the broader community.</p>
                </div>
                <div class="about-image">
                    <img src="https://via.placeholder.com/500x400/6366f1/ffffff?text=Event+Planning" alt="Students collaborating and planning an event on a laptop, illustrating event management.">
                </div>
            </div>
        </div>
    </section>

    <section id="events" class="events" aria-labelledby="events-title">
        <div class="container">
            <h2 id="events-title" class="section-title">Upcoming Events</h2>
            <div class="events-grid">
                <div class="event-card">
                    <div class="event-image" aria-hidden="true">
                        <i class="fas fa-code"></i>
                    </div>
                    <div class="event-content">
                        <div class="event-date">March 15, 2024</div>
                        <h3 class="event-title">TechFest 2024</h3>
                        <p>Annual technical festival featuring coding competitions, robotics, and innovation showcases.</p>
                    </div>
                </div>
                <div class="event-card">
                    <div class="event-image" aria-hidden="true">
                        <i class="fas fa-music"></i>
                    </div>
                    <div class="event-content">
                        <div class="event-date">April 2, 2024</div>
                        <h3 class="event-title">Cultural Extravaganza</h3>
                        <p>Celebrate diversity through music, dance, drama, and art performances by talented students.</p>
                    </div>
                </div>
                <div class="event-card">
                    <div class="event-image" aria-hidden="true">
                        <i class="fas fa-graduation-cap"></i>
                    </div>
                    <div class="event-content">
                        <div class="event-date">April 20, 2024</div>
                        <h3 class="event-title">Academic Symposium</h3>
                        <p>Research presentations, guest lectures, and academic discussions across various disciplines.</p>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <section id="queries" class="queries" aria-labelledby="queries-title">
        <div class="container">
            <h2 id="queries-title" class="section-title">Have Questions?</h2>
            <form class="contact-form" id="queryForm" aria-label="Contact Us Form">
                <div class="form-group">
                    <label for="queryName">Full Name</label>
                    <input type="text" id="queryName" name="name" required aria-required="true">
                </div>
                <div class="form-group">
                    <label for="queryEmail">Email Address</label>
                    <input type="email" id="queryEmail" name="email" required aria-required="true">
                </div>
                <div class="form-group">
                    <label for="querySubject">Subject</label>
                    <input type="text" id="querySubject" name="subject" required aria-required="true">
                </div>
                <div class="form-group">
                    <label for="queryMessage">Your Query</label>
                    <textarea id="queryMessage" name="message" rows="5" required aria-required="true"></textarea>
                </div>
                <button type="submit" class="submit-btn">Send Query</button>
            </form>
        </div>
    </section>

    <footer class="footer" role="contentinfo">
        <div class="container">
            <p>&copy; 2024 Technokratos. All rights reserved. | Empowering College Events</p>
        </div>
    </footer>

    <script>
        document.addEventListener('DOMContentLoaded', () => {
            // Mobile menu toggle
            const mobileMenu = document.querySelector('.mobile-menu');
            const navMenu = document.querySelector('.nav-menu');

            mobileMenu.addEventListener('click', () => {
                navMenu.classList.toggle('active');
                mobileMenu.classList.toggle('active'); // Add active class to mobile menu for animation
                const isExpanded = mobileMenu.classList.contains('active');
                mobileMenu.setAttribute('aria-expanded', isExpanded);
            });

            // Close mobile menu when a nav link is clicked
            navMenu.querySelectorAll('.nav-link').forEach(link => {
                link.addEventListener('click', () => {
                    if (navMenu.classList.contains('active')) {
                        navMenu.classList.remove('active');
                        mobileMenu.classList.remove('active');
                        mobileMenu.setAttribute('aria-expanded', 'false');
                    }
                });
            });

            // Smooth scrolling for navigation links
            document.querySelectorAll('a[href^="#"]').forEach(anchor => {
                anchor.addEventListener('click', function (e) {
                    e.preventDefault();
                    const targetId = this.getAttribute('href');
                    const targetElement = document.querySelector(targetId);
                    if (targetElement) {
                        // Get the height of the fixed navbar
                        const navbarHeight = document.querySelector('.navbar').offsetHeight;
                        // Calculate position to scroll to, accounting for the navbar
                        const offsetTop = targetElement.getBoundingClientRect().top + window.scrollY - navbarHeight;

                        window.scrollTo({
                            top: offsetTop,
                            behavior: 'smooth'
                        });
                    }
                });
            });

            // Navbar background change on scroll
            const navbar = document.querySelector('.navbar');
            const changeNavbarBackground = () => {
                if (window.scrollY > 50) { // Adjusted scroll threshold
                    navbar.style.background = 'rgba(255, 255, 255, 0.98)';
                    navbar.style.boxShadow = '0 4px 25px rgba(0, 0, 0, 0.15)'; /* More prominent shadow on scroll */
                } else {
                    navbar.style.background = 'rgba(255, 255, 255, 0.95)';
                    navbar.style.boxShadow = '0 2px 20px rgba(0, 0, 0, 0.1)';
                }
            };
            window.addEventListener('scroll', changeNavbarBackground);
            changeNavbarBackground(); // Call once on load to set initial state

            // Queries Form Submission (Client-Side Simulation)
            const queryForm = document.getElementById('queryForm');
            if (queryForm) { // Check if form exists
                queryForm.addEventListener('submit', function(e) {
                    e.preventDefault();

                    const formData = new FormData(this);
                    const name = formData.get('name');
                    const email = formData.get('email');
                    const subject = formData.get('subject');
                    const message = formData.get('message');

                    // Basic client-side validation
                    if (!name || !email || !subject || !message) {
                        alert('Please fill in all fields.');
                        return;
                    }

                    // Simulate server response and user feedback
                    alert(Thank you, ${name}! Your query has been submitted successfully. We'll get back to you at ${email} soon.);

                    this.reset(); // Reset form fields
                });
            }


            // Scroll Animations with Intersection Observer
            const observerOptions = {
                threshold: 0.15, // Trigger when 15% of the element is visible
                rootMargin: '0px 0px -50px 0px' // Start animation slightly before entering viewport fully
            };

            const observer = new IntersectionObserver((entries) => {
                entries.forEach(entry => {
                    if (entry.isIntersecting) {
                        entry.target.classList.add('fade-in-up-animated');
                        observer.unobserve(entry.target); // Stop observing once animated
                    }
                });
            }, observerOptions);

            // Apply initial styles and observe elements for animation
            document.querySelectorAll('.feature-card, .event-card, .about-text').forEach(el => {
                el.style.opacity = '0';
                el.style.transform = 'translateY(40px)'; // Increased translateY for a more noticeable effect
                el.style.transition = 'opacity 0.8s ease-out, transform 0.8s ease-out'; // Slower, smoother transition
                observer.observe(el);
            });
            // Add a class for the actual animation
            const style = document.createElement('style');
            style.innerHTML = `
                .fade-in-up-animated {
                    opacity: 1 !important;
                    transform: translateY(0) !important;
                }
            `;
            document.head.appendChild(style);


            // Add typing effect to hero title
            const heroTitle = document.querySelector('.hero h1');
            if (heroTitle) { // Check if element exists
                const titleText = heroTitle.textContent;
                heroTitle.textContent = ''; // Clear text initially

                let i = 0;
                const typeWriter = () => {
                    if (i < titleText.length) {
                        heroTitle.textContent += titleText.charAt(i);
                        i++;
                        setTimeout(typeWriter, 70); // Slightly faster typing
                    }
                };

                // Start typing effect after page load
                window.addEventListener('load', () => {
                    setTimeout(typeWriter, 800); // Start a bit sooner
                });
            }
        });
    </script>
</body>
</html>
